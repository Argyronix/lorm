__pycache__/
*.pyc
# Bodies passed to `gh release create --notes-file` / `gh issue create
# --body-file`. Scratch by design: once filed, the durable copy lives on
# GitHub (or, for releases, in CHANGELOG.md), and a tracked second copy here
# would drift from it.
release-notes-v*.md
issue-*.md
